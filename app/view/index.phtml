<?php include __DIR__ . '/partials/head.phtml'; ?>
<?php include __DIR__ . '/partials/nav.phtml'; ?>

<div class="hero">
    <div class="hero__grid">
        <div>
            <h1 class="hero__title">A standalone PDF library for PHP.</h1>
            <p class="hero__lead">Working with PDFs in PHP usually means collecting libraries. One writes documents but can&rsquo;t read them. Another reads but can&rsquo;t write. A third renders HTML. A fourth merges.</p>
            <p class="hero__lead hero__lead--ink">Pop PDF does all of it &mdash; through one object model, one API, <em class="hero__em">one dependency</em>.</p>

            <div class="hero__actions">
                <?php $terminalSmall = true; include __DIR__ . '/partials/terminal.phtml'; ?>
                <a href="#map" class="btn-outline">See the code &rarr;</a>
            </div>
            <div class="hero__meta">PHP 8.4+ &middot; BSD-3-Clause &middot; in development since 2009</div>
        </div>

        <div class="stack">
            <div class="eyebrow stack__eyebrow">The usual stack</div>
            <div>
                <div class="stack__row">
                    <span class="stack__name">a generator library</span>
                    <span class="stack__note">writes, can&rsquo;t read</span>
                </div>
                <div class="stack__row">
                    <span class="stack__name">a parser library</span>
                    <span class="stack__note">reads, can&rsquo;t write</span>
                </div>
                <div class="stack__row">
                    <span class="stack__name">an HTML renderer</span>
                    <span class="stack__note">one job</span>
                </div>
                <div class="stack__row">
                    <span class="stack__name">a merge utility</span>
                    <span class="stack__note">one job</span>
                </div>
            </div>
            <div class="stack__aside">Four dependencies. Four release cycles to manage.</div>
            <div class="eyebrow stack__answer-eyebrow">Or, just one</div>
            <div class="stack__answer">
                <?php $markSize = 26; $markStroke = '2.9'; include __DIR__ . '/partials/mark.phtml'; ?>
                <div>
                    <div class="stack__answer-title">Pop PDF</div>
                    <div class="stack__answer-sub">Many features. One object model.</div>
                </div>
            </div>
        </div>
    </div>
</div>

<section class="tagline">
    <h2 class="tagline__title">As pixel&#8209;perfect or as simple as you want to be.</h2>
</section>

<div id="map" class="map">
    <div class="map__row">

        <div class="map__col">
            <div class="map__head">
                <span class="map__num">01</span>
                <h2 class="map__title">Build</h2>
            </div>
            <p class="map__lead">A one-page PDF is a few lines. Or hand it HTML and let it do the work for you.</p>
<?php
$codeFile = 'generate.php';
$code     = <<<'HTML'
<span class="tok-var">$doc</span> = <span class="tok-kw">new</span> <span class="tok-type">Pop\Pdf\Document</span>();
<span class="tok-var">$page</span> = <span class="tok-var">$doc</span>-&gt;<span class="tok-fn">createPage</span>(<span class="tok-type">Page</span>::<span class="tok-fn">LETTER</span>);
<span class="tok-var">$page</span>-&gt;<span class="tok-fn">addText</span>(
    <span class="tok-str">'Hello'</span>, <span class="tok-type">Font</span>::<span class="tok-fn">ARIAL</span>, x: <span class="tok-num">50</span>, y: <span class="tok-num">742</span>
);
HTML;
include __DIR__ . '/partials/code-window.phtml';
?>
            <div class="map__list">
                <div>Simple &amp; advanced document generation</div>
                <div>HTML&#8209;to&#8209;PDF</div>
                <div>Simple &amp; complex fonts</div>
                <div>Annotations &amp; links</div>
                <div>Drawing paths</div>
                <div>Forms</div>
                <div>Encryption &amp; permissions</div>
            </div>
            <a href="/build" class="map__link">/build &rarr;</a>
        </div>

        <div class="map__col">
            <div class="map__head">
                <span class="map__num">02</span>
                <h2 class="map__title">Import</h2>
            </div>
            <p class="map__lead">An imported PDF comes back as the same objects you would have built yourself.</p>
<?php
$codeFile = 'modify.php';
$code     = <<<'HTML'
<span class="tok-var">$doc</span> = <span class="tok-type">Pdf</span>::<span class="tok-fn">importFromFile</span>(
    <span class="tok-str">'contract.pdf'</span>, pages: [<span class="tok-num">2</span>, <span class="tok-num">4</span>, <span class="tok-num">6</span>]
);

<span class="tok-type">Pdf</span>::<span class="tok-fn">writeToFile</span>(<span class="tok-var">$doc</span>, <span class="tok-str">'out.pdf'</span>);
HTML;
include __DIR__ . '/partials/code-window.phtml';
?>
            <div class="map__list">
                <div>Import and modify PDFs</div>
                <div>Merge PDFs</div>
                <div>Open a protected PDF</div>
                <div>Build a PDF from images</div>
            </div>
            <a href="/import" class="map__link">/import &rarr;</a>
        </div>

        <div class="map__col">
            <div class="map__head">
                <span class="map__num">03</span>
                <h2 class="map__title">Extract</h2>
            </div>
            <p class="map__lead">Native text extraction, embedded fonts included &mdash; and a way to spot a scan before you read it.</p>
<?php
$codeFile = 'extract.php';
$code     = <<<'HTML'
<span class="tok-var">$text</span> = <span class="tok-type">Pdf</span>::<span class="tok-fn">extractTextFromFile</span>(
    <span class="tok-str">'report.pdf'</span>, pages: [<span class="tok-num">1</span>, <span class="tok-num">2</span>, <span class="tok-num">3</span>]
);

<span class="tok-type">Pdf</span>::<span class="tok-fn">isImageOnlyDocument</span>(<span class="tok-str">'scan.pdf'</span>);
HTML;
include __DIR__ . '/partials/code-window.phtml';
?>
            <div class="map__list">
                <div>Extract text</div>
                <div>Image&#8209;only detection</div>
                <div>Extract as images</div>
            </div>
            <a href="/extract" class="map__link">/extract &rarr;</a>
        </div>
    </div>
</div>

<?php include __DIR__ . '/partials/install-band.phtml'; ?>
<?php include __DIR__ . '/partials/footer.phtml'; ?>
