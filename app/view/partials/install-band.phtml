<?php
/**
 * Install band. Optional: $installClass - extra class on the wrapper
 * ('install--after-features' where it stands in for the next-page card).
 */
$installClass = $installClass ?? '';
?>
<div class="install <?=$installClass ?>">
    <div class="install__panel">
        <div>
            <h3 class="install__title">One install, all of it.</h3>
            <p class="install__sub">PHP 8.4+. Optional extensions: <span class="code-inline">gd</span>, <span class="code-inline">imagick</span>, <span class="code-inline">zlib</span>, <span class="code-inline">mbstring</span>.</p>
        </div>
        <?php include __DIR__ . '/terminal.phtml'; ?>
    </div>
</div>
<?php $installClass = ''; ?>
